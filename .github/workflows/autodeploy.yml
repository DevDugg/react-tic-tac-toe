name: Deployment

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: github-pages
    concurrency: production
    steps:
      - name: deploy
        run: npm run deploy "./"
